# Orchestrator Repo

This repository manages the service submodules for the roster and tactical optimization services.

## Included submodules

- `roster-service`
- `tactical-optimization-service`

## Docker compose

- `docker-compose.yml` orchestrates the roster service components from the `roster-service` submodule.
